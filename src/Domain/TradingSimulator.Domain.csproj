<Project Sdk="Microsoft.NET.Sdk">

  <!-- Folders only until aggregates are implemented; no compile items yet. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>

</Project>
