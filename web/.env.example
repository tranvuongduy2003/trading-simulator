# Copy to .env for local Vite dev outside Aspire (API HTTPS port from AppHost)
VITE_API_URL=https://localhost:8000
# Set to 1 to serve Vite over HTTPS when not running under Aspire
# VITE_DEV_HTTPS=1
